/* ─── Owned Software: the category's canonical language ───
   Coined publicly 2026-08-28 (blog: what-is-owned-software). ONE definition,
   used verbatim on /owned-software, /owned-software-vs-saas,
   /owned-software-test, the blog post, and llms.txt so no surface ever
   drifts. The definition is deliberately COMPANY-NEUTRAL: a category other
   builders can qualify under is a category; a definition only Found It can
   meet is product branding. Found It's METHOD is stated separately. */

/** The category definition, verbatim everywhere. */
export const OWNED_SOFTWARE_DEFINITION =
  'Owned software is business software the business owns outright: the code and the data. The business can keep running it, change it, or hand it to another builder without the original vendor’s permission. If the software company disappears tomorrow, the software is still there, still running, still the owner’s property.';

/** The equation, verbatim everywhere. */
export const OWNED_SOFTWARE_EQUATION = 'SaaS = rented software. Owned software = the opposite.';

/** The one-question diagnostic the whole category hangs on. */
export const OWNED_SOFTWARE_TEST_QUESTION = 'If you stop paying, what is left?';

/** Found It's version of the category — method, not definition. */
export const FOUND_IT_METHOD =
  'Found It builds the bespoke version: fitted to one business the way a suit is cut to one body, old records migrated in instead of abandoned, running beside the system it replaces until the numbers match and the owner says go.';
